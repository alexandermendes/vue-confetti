sudo: required

language: node_js

node_js:
  - '10'

install:
  - npm install

script:
  - npm run test
  - npm run build

# Deploy demo to GitHub pages
deploy:
  provider: pages
  local-dir: dist
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
